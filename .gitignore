# Secrets — never commit
**/.env
!**/.env.example
caddy/caddy.env

# Python
__pycache__/
*.pyc
.venv/
venv/
*.db
.pytest_cache/

# FastAPI-served SPA build (copied in at build time)
backend/static/

# Node / Vite / build artifacts
node_modules/
dist/
build/
*.local
*.tsbuildinfo

# Local scratch
build-test/
poc-source/
poc-check/

# OS / editor
.DS_Store
Thumbs.db
