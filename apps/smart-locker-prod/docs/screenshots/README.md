# Screenshots

Drop the three module screenshots here with these exact filenames so the
production README renders them:

| Filename | Module | Subdomain |
|----------|--------|-----------|
| `admin.png` | Master System View (Admin) | `admin.smart-locker.yeng.click` |
| `agent.png` | Delivery Agent | `agent.smart-locker.yeng.click` |
| `customer.png` | Customer Pickup | `customer.smart-locker.yeng.click` |

PNG or JPG are both fine; if you use `.jpg`, update the image links in
`apps/smart-locker-prod/README.md` accordingly.
